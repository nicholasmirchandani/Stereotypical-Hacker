#include "Player.h"

Player::Player() {
    //Default Constructor
    //Only setting these attributes since others are variable and are set as needed
    score = 0;
    currentServer = nullptr;
    playingGame = false;
}